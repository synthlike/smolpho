# smolpho

smolpho is an extremely small lending protocol inspired by Morpho Blue.
The main purpose is to be an educational resource.

smolpho currently supports:
  - isolated markets - one immutable market per contract deployment
