version: "3"

# TODO: add sources/generates

vars:
  CONTRACTS_DIR: contracts
  INDEXER_DIR: indexer
  ARTIFACT: contracts/out/Smolpho.sol/Smolpho.json
  ABI: indexer/internal/bindings/smolpho.abi.json
  BINDINGS: indexer/internal/bindings/smolpho.go
  TOOLS_DIR: "{{.ROOT_DIR}}/.tools/bin"
  ABIGEN: "{{.TOOLS_DIR}}/abigen"

  # Deterministic local demo configuration. The mnemonic and private key are
  # Anvil's public test-only credentials and must never be used on a real network.
  DEMO_ANVIL_HOST: 127.0.0.1
  DEMO_ANVIL_PORT: 8545
  DEMO_ANVIL_RPC: "http://{{.DEMO_ANVIL_HOST}}:{{.DEMO_ANVIL_PORT}}"
  DEMO_MNEMONIC: "test test test test test test test test test test test junk"
  DEMO_USER: "0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266"
  DEMO_PRIVATE_KEY: "0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80"
  DEMO_LOAN_TOKEN: "0x5FbDB2315678afecb367f032d93F642f64180aa3"
  DEMO_COLLATERAL_TOKEN: "0xe7f1725E7734CE288F8367e1Bb143E90bb3F0512"
  DEMO_ORACLE: "0x9fE46736679d2D9a65F0992F2272dE9f3c7fa6e0"
  DEMO_SMOLPHO: "0xCf7Ed3AccA5a467e9e704C703E8D87F634fB0Fc9"
  DEMO_SMOLPHO_DEPLOYMENT_BLOCK: 4
  DEMO_API_LISTEN: "127.0.0.1:8080"
  DEMO_API_CORS_ORIGIN: "http://localhost:5173"
  DEMO_API_DATABASE: "smolpho-indexer.sqlite"

tasks:
  install:
    desc: Download project dependencies, including the pinned abigen tool
    cmds:
      - task: tools:install
      - task: indexer:install
      - task: web:install

  tools:install:
    desc: Install pinned development tools inside the repository
    env:
      GOBIN: "{{.TOOLS_DIR}}"
    cmds:
      - go install github.com/ethereum/go-ethereum/cmd/abigen@v1.17.5

  build:
    desc: Build contracts, refresh ABI + bindings, and build the indexer
    cmds:
      - task: contracts:build
      - task: indexer:abi
      - task: indexer:bindings
      - task: indexer:build

  test:
    desc: Run all test suites
    cmds:
      - task: contracts:test
      - task: indexer:test

  contracts:build:
    desc: Compile production Solidity contracts with Foundry
    dir: "{{.CONTRACTS_DIR}}"
    cmds:
      - forge build --skip test

  contracts:test:
    desc: Run the Solidity test suite
    dir: "{{.CONTRACTS_DIR}}"
    cmds:
      - forge test

  indexer:abi:
    desc: Extract the Smolpho ABI from the Foundry artifact into the indexer
    cmds:
      - jq '.abi' {{.ARTIFACT}} > {{.ABI}}

  indexer:install:
    desc: Download the Go indexer dependencies
    dir: "{{.INDEXER_DIR}}"
    cmds:
      - go mod download

  indexer:bindings:
    desc: Generate Go contract bindings from the ABI
    preconditions:
      - sh: test -x "{{.ABIGEN}}"
        msg: "abigen is not installed; run 'task install' first"
    cmds:
      - "{{.ABIGEN}} --abi {{.ABI}} --pkg bindings --type Smolpho --out {{.BINDINGS}}"

  indexer:build:
    desc: Build the Go indexer
    dir: "{{.INDEXER_DIR}}"
    cmds:
      - go build ./...

  indexer:test:
    desc: Run the Go indexer test suite
    dir: "{{.INDEXER_DIR}}"
    cmds:
      - go test ./...

  web:install:
    desc: Install the locked web application dependencies
    dir: web
    cmds:
      - npm ci

  web:dev:
    desc: Start the Smolpho Lens development server
    dir: web
    cmds:
      - npm run dev

  web:build:
    desc: Type-check and build the Smolpho Lens frontend
    dir: web
    cmds:
      - npm run build

  demo:anvil:
    desc: Start a fresh deterministic Anvil node for the manual indexer demo
    cmds:
      - >-
        anvil
        --host {{.DEMO_ANVIL_HOST}}
        --port {{.DEMO_ANVIL_PORT}}
        --chain-id 31337
        --quiet
        --mnemonic "{{.DEMO_MNEMONIC}}"

  demo:deploy:
    desc: Deploy the deterministic demo contracts to a fresh Anvil node
    dir: "{{.CONTRACTS_DIR}}"
    cmds:
      - |
        nonce="$(cast nonce --rpc-url {{.DEMO_ANVIL_RPC}} {{.DEMO_USER}})"
        if [ "$nonce" != "0" ]; then
          echo "demo deployer nonce is $nonce, expected 0; restart Anvil"
          exit 1
        fi
      - >-
        forge create --broadcast
        --rpc-url {{.DEMO_ANVIL_RPC}}
        --private-key {{.DEMO_PRIVATE_KEY}}
        test/mocks/MockERC20.sol:MockERC20
        --constructor-args "Loan Token" "LOAN"
      - >-
        forge create --broadcast
        --rpc-url {{.DEMO_ANVIL_RPC}}
        --private-key {{.DEMO_PRIVATE_KEY}}
        test/mocks/MockERC20.sol:MockERC20
        --constructor-args "Collateral Token" "COLL"
      - >-
        forge create --broadcast
        --rpc-url {{.DEMO_ANVIL_RPC}}
        --private-key {{.DEMO_PRIVATE_KEY}}
        test/mocks/MockOracle.sol:MockOracle
        --constructor-args 1000000000000000000
      - >-
        forge create --broadcast
        --rpc-url {{.DEMO_ANVIL_RPC}}
        --private-key {{.DEMO_PRIVATE_KEY}}
        src/Smolpho.sol:Smolpho
        --constructor-args
        {{.DEMO_LOAN_TOKEN}}
        {{.DEMO_COLLATERAL_TOKEN}}
        {{.DEMO_ORACLE}}
        800000000000000000
        0
        1100000000000000000
      - |
        code="$(cast code --rpc-url {{.DEMO_ANVIL_RPC}} {{.DEMO_SMOLPHO}})"
        block="$(cast block-number --rpc-url {{.DEMO_ANVIL_RPC}})"
        if [ "$code" = "0x" ] || [ "$block" != "{{.DEMO_SMOLPHO_DEPLOYMENT_BLOCK}}" ]; then
          echo "unexpected deployment result; restart Anvil and run demo:deploy again"
          exit 1
        fi
        echo "Smolpho deployed at {{.DEMO_SMOLPHO}} in block $block"

  demo:indexer:
    desc: Follow the deterministic Anvil deployment and print indexed state
    dir: "{{.INDEXER_DIR}}"
    cmds:
      - |
        code="$(cast code --rpc-url {{.DEMO_ANVIL_RPC}} {{.DEMO_SMOLPHO}})"
        if [ "$code" = "0x" ]; then
          echo "Smolpho is not deployed; run demo:deploy first"
          exit 1
        fi
      - >-
        go run ./cmd/indexer-cli
        -rpc {{.DEMO_ANVIL_RPC}}
        -contract {{.DEMO_SMOLPHO}}
        -deployment-block {{.DEMO_SMOLPHO_DEPLOYMENT_BLOCK}}
        -interval 250ms
        -follow

  demo:api:
    desc: Follow the deterministic Anvil deployment and serve the HTTP API
    dir: "{{.INDEXER_DIR}}"
    cmds:
      - |
        code="$(cast code --rpc-url {{.DEMO_ANVIL_RPC}} {{.DEMO_SMOLPHO}})"
        if [ "$code" = "0x" ]; then
          echo "Smolpho is not deployed; run demo:deploy first"
          exit 1
        fi
      - >-
        go run ./cmd/indexer-api
        -rpc {{.DEMO_ANVIL_RPC}}
        -contract {{.DEMO_SMOLPHO}}
        -deployment-block {{.DEMO_SMOLPHO_DEPLOYMENT_BLOCK}}
        -database {{.DEMO_API_DATABASE}}
        -listen {{.DEMO_API_LISTEN}}
        -cors-origin {{.DEMO_API_CORS_ORIGIN}}
        -interval 250ms
        -follow

  demo:scenario:
    desc: Create one supplier and four borrower/collateral positions on Anvil
    cmds:
      - echo "Minting 1,000 loan tokens"
      - >-
        cast send --quiet --rpc-url {{.DEMO_ANVIL_RPC}} --private-key {{.DEMO_PRIVATE_KEY}}
        {{.DEMO_LOAN_TOKEN}} "mint(address,uint256)" {{.DEMO_USER}} 1000000000000000000000
      - echo "Approving and supplying 100 loan tokens"
      - >-
        cast send --quiet --rpc-url {{.DEMO_ANVIL_RPC}} --private-key {{.DEMO_PRIVATE_KEY}}
        {{.DEMO_LOAN_TOKEN}} "approve(address,uint256)" {{.DEMO_SMOLPHO}} 100000000000000000000
      - >-
        cast send --quiet --rpc-url {{.DEMO_ANVIL_RPC}} --private-key {{.DEMO_PRIVATE_KEY}}
        {{.DEMO_SMOLPHO}} "supply(uint256)" 100000000000000000000
      - sleep 1
      - echo "Minting, approving, and supplying 20 collateral tokens"
      - >-
        cast send --quiet --rpc-url {{.DEMO_ANVIL_RPC}} --private-key {{.DEMO_PRIVATE_KEY}}
        {{.DEMO_COLLATERAL_TOKEN}} "mint(address,uint256)" {{.DEMO_USER}} 20000000000000000000
      - >-
        cast send --quiet --rpc-url {{.DEMO_ANVIL_RPC}} --private-key {{.DEMO_PRIVATE_KEY}}
        {{.DEMO_COLLATERAL_TOKEN}} "approve(address,uint256)" {{.DEMO_SMOLPHO}} 20000000000000000000
      - >-
        cast send --quiet --rpc-url {{.DEMO_ANVIL_RPC}} --private-key {{.DEMO_PRIVATE_KEY}}
        {{.DEMO_SMOLPHO}} "supplyCollateral(uint256)" 20000000000000000000
      - sleep 1
      - echo "Withdrawing half of the minted supply shares"
      - >-
        cast send --quiet --rpc-url {{.DEMO_ANVIL_RPC}} --private-key {{.DEMO_PRIVATE_KEY}}
        {{.DEMO_SMOLPHO}} "withdraw(uint256)" 50000000000000000000000000
      - sleep 1
      - |
        mnemonic="{{.DEMO_MNEMONIC}}"
        for fixture in \
          "1:20000000000000000000:8000000000000000000" \
          "2:30000000000000000000:12000000000000000000" \
          "3:10000000000000000000:4000000000000000000" \
          "4:5000000000000000000:0"
        do
          account_index="${fixture%%:*}"
          remainder="${fixture#*:}"
          collateral="${remainder%%:*}"
          borrow="${remainder#*:}"
          private_key="$(cast wallet private-key "$mnemonic" "$account_index")"
          user="$(cast wallet address --private-key "$private_key")"

          echo "Creating position for Anvil account $account_index ($user)"
          cast send --quiet --rpc-url {{.DEMO_ANVIL_RPC}} --private-key "$private_key" \
            {{.DEMO_COLLATERAL_TOKEN}} "mint(address,uint256)" "$user" "$collateral"
          cast send --quiet --rpc-url {{.DEMO_ANVIL_RPC}} --private-key "$private_key" \
            {{.DEMO_COLLATERAL_TOKEN}} "approve(address,uint256)" {{.DEMO_SMOLPHO}} "$collateral"
          cast send --quiet --rpc-url {{.DEMO_ANVIL_RPC}} --private-key "$private_key" \
            {{.DEMO_SMOLPHO}} "supplyCollateral(uint256)" "$collateral"
          if [ "$borrow" != "0" ]; then
            cast send --quiet --rpc-url {{.DEMO_ANVIL_RPC}} --private-key "$private_key" \
              {{.DEMO_SMOLPHO}} "borrow(uint256)" "$borrow"
          fi
          sleep 1
        done
